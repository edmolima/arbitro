{
  "name": "arbitro-monorepo",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/edmolima/arbitro.git"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -F @edmolima/arbitro build && pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -F @edmolima/arbitro build && pnpm -F playground dev",
    "eval": "pnpm -F @arbitro/eval eval"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
