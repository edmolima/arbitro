{
  "name": "playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "batch": "tsx src/index.ts --batch",
    "openrouter": "tsx src/openrouter.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm -F @edmolima/arbitro build"
  },
  "dependencies": {
    "@edmolima/arbitro": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "vitest": "^4.1.10",
    "typescript": "^5.6.0",
    "@types/node": "^22.10.0"
  }
}
